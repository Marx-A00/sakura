---
phase: 06-fix-data-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/sakura/data/workout/OrgWorkoutRepository.kt
  - app/src/test/java/com/sakura/orgengine/OrgParserTest.kt
  - app/src/main/java/com/sakura/orgengine/OrgParser.kt
autonomous: true

must_haves:
  truths:
    - "WorkoutSession.templateName is non-null after cold-start parse when splitDay was persisted"
    - "Dashboard and workout screen display the correct template name (e.g. 'Monday — Lift (Heavy Compounds)') instead of null/Freestyle after cold start"
    - "SetLog.isPr round-trips through OrgSchema.formatSetEntry -> OrgParser correctly (true written, true read back)"
    - "PR badge renders on sets that were detected as PRs after app restart"
  artifacts:
    - path: "app/src/main/java/com/sakura/data/workout/OrgWorkoutRepository.kt"
      provides: "templateName derivation from splitDay in toWorkoutSession()"
      contains: "templateName = splitDayParsed?.displayName"
    - path: "app/src/test/java/com/sakura/orgengine/OrgParserTest.kt"
      provides: "isPr round-trip verification test"
      contains: "isPr"
  key_links:
    - from: "OrgWorkoutRepository.toWorkoutSession()"
      to: "WorkoutSession.templateName"
      via: "splitDayParsed?.displayName derivation"
      pattern: "templateName = splitDayParsed"
    - from: "OrgSchema.formatSetEntry()"
      to: "OrgParser set-level drawer"
      via: ":is_pr: property round-trip"
      pattern: "PROP_IS_PR"
---

<objective>
Fix two workout data bugs: (1) templateName is null after cold start because toWorkoutSession() never derives it from the persisted splitDay, and (2) isPr badge may not persist correctly across app restarts.

Purpose: Dashboard shows "Freestyle" instead of the real template name after restart; PR badges vanish after cold start. Both degrade the workout tracking experience.
Output: Patched OrgWorkoutRepository with templateName derivation, verified isPr round-trip with unit test, any necessary parser fix applied.
</objective>

<execution_context>
@/Users/marcosandrade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcosandrade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fix-data-bugs/06-RESEARCH.md

@app/src/main/java/com/sakura/data/workout/OrgWorkoutRepository.kt
@app/src/main/java/com/sakura/data/workout/WorkoutSession.kt
@app/src/main/java/com/sakura/orgengine/OrgSchema.kt
@app/src/main/java/com/sakura/orgengine/OrgParser.kt
@app/src/main/java/com/sakura/orgengine/OrgModels.kt
@app/src/test/java/com/sakura/orgengine/OrgParserTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix templateName derivation in toWorkoutSession()</name>
  <files>app/src/main/java/com/sakura/data/workout/OrgWorkoutRepository.kt</files>
  <action>
In `OrgWorkoutRepository.kt`, find the `OrgDateSection.toWorkoutSession()` private extension function (around line 518).

The current `WorkoutSession` constructor call is missing `templateName`. Add `templateName = splitDayParsed?.displayName` to the constructor:

```kotlin
return WorkoutSession(
    date = this.date,
    splitDay = splitDayParsed,
    templateName = splitDayParsed?.displayName,   // derive from persisted splitDay
    exercises = exerciseList,
    durationMin = this.durationMin ?: 0,
    isComplete = this.complete
)
```

This is a single-line addition. The `splitDayParsed` variable is already computed on the line above from `this.splitDay?.let { SplitDay.fromLabel(it) }`. The `SplitDay.displayName` property returns the full human-readable string like `"Monday — Lift (Heavy Compounds)"`.

Do NOT modify any other part of this function or the file. The calendar logic in `WorkoutLogViewModel.loadCalendar()` uses its own abbreviated fallback and will not regress.
  </action>
  <verify>
Build the project:
```bash
cd /Users/marcosandrade/roaming/projects/sakura && JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug 2>&1 | tail -5
```
Confirm BUILD SUCCESSFUL.
  </verify>
  <done>
The `toWorkoutSession()` constructor includes `templateName = splitDayParsed?.displayName`. After cold-start parse, `WorkoutSession.templateName` is non-null when splitDay was persisted in the org file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify and fix isPr round-trip persistence</name>
  <files>
    app/src/test/java/com/sakura/orgengine/OrgParserTest.kt
    app/src/main/java/com/sakura/orgengine/OrgParser.kt
  </files>
  <action>
**Step 1: Add a unit test** to `OrgParserTest.kt` that verifies the isPr round-trip:

Write a test that:
1. Creates an `OrgSetEntry` with `isPr = true` (and other standard fields: setNumber=1, reps=5, weight=100.0, unit="kg")
2. Calls `OrgSchema.formatSetEntry(setEntry)` to produce the org text
3. Embeds that text inside a minimal valid workout org file string (with date heading, exercise heading, and the set)
4. Parses the full string with `OrgParser.parse(text, OrgParser.Mode.WORKOUT)`
5. Asserts that the parsed `OrgSetEntry.isPr` is `true`

This test will confirm or deny whether the round-trip works. The test should be named something like `test parse workout isPr round trip`.

**Step 2: Run the test.** If it passes, the write+read path is correct and the bug is elsewhere (likely in the ViewModel's PR detection not firing, which is a known edge case with `findPersonalBest` returning null on first-ever set). If it fails, inspect the parser's `:is_pr:` handling.

**Step 3: If the test fails,** the most likely cause is whitespace handling in the PROPERTY_REGEX or `toBooleanStrictOrNull()` receiving a trimmed vs untrimmed value. Fix the parser accordingly. The regex is `Regex("""^:(\w+):\s+(.+)$""")` which captures the value with `.+` (greedy, may include trailing whitespace). Ensure the value is `.trim()`ed before `toBooleanStrictOrNull()`.

Check line 287 in `OrgParser.kt`:
```kotlin
isPr = drawerProperties[OrgSchema.PROP_IS_PR]?.toBooleanStrictOrNull() ?: false,
```
If the property value has trailing whitespace (from the regex capture), `"true "` would cause `toBooleanStrictOrNull()` to return `null`, defaulting to `false`. If this is the case, add `.trim()` before `.toBooleanStrictOrNull()`.

Also check the property accumulation code to see if values are trimmed when stored in `drawerProperties`. Look for where `drawerProperties[key] = value` is set — if the regex match group is not trimmed, add `.trim()`.

**Important:** The test is the diagnostic. Write it first, run it, then fix only if it fails.
  </action>
  <verify>
Run the tests:
```bash
cd /Users/marcosandrade/roaming/projects/sakura && JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew test 2>&1 | tail -10
```
All tests pass, including the new isPr round-trip test.
  </verify>
  <done>
A unit test confirms `isPr = true` survives the `formatSetEntry -> parse` round-trip. If a parser bug was found (e.g., untrimmed value), it is fixed. The isPr badge will render correctly when reading workout history from the org file.
  </done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` succeeds (no compilation errors from templateName change)
2. `./gradlew test` succeeds (all existing tests pass + new isPr round-trip test passes)
3. `toWorkoutSession()` constructor includes `templateName = splitDayParsed?.displayName`
4. OrgParserTest contains a test verifying isPr=true round-trip
</verification>

<success_criteria>
- WorkoutSession.templateName is derived from splitDay.displayName on cold-start parse
- isPr=true written by formatSetEntry is correctly read back by OrgParser
- All unit tests pass
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-data-bugs/06-01-SUMMARY.md`
</output>
