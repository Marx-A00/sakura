---
phase: 04-dashboard-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/com/sakura/navigation/Routes.kt
  - app/src/main/java/com/sakura/sync/SyncBackend.kt
  - app/src/main/java/com/sakura/sync/SyncthingFileBackend.kt
  - app/src/main/java/com/sakura/features/dashboard/DashboardUiState.kt
  - app/src/main/java/com/sakura/features/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/sakura/features/dashboard/DashboardScreen.kt
  - app/src/main/java/com/sakura/features/dashboard/FoodProgressCard.kt
  - app/src/main/java/com/sakura/features/dashboard/WorkoutSummaryCard.kt
  - app/src/main/java/com/sakura/features/dashboard/SyncStatusBadge.kt
  - app/src/main/java/com/sakura/navigation/AppNavHost.kt
  - app/src/main/java/com/sakura/di/AppContainer.kt
  - app/src/main/java/com/sakura/features/foodlog/FoodLogScreen.kt
  - app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
autonomous: false

must_haves:
  truths:
    - "Home tab shows today's macro progress (logged vs. target) and today's workout summary in a single scrollable view"
    - "Bottom navigation works across all tabs (FOOD, WORKOUT, HOME, SETTINGS) without back-stack accumulation"
    - "Sync status pill badge in home header shows 'Synced' with timestamp or conflict warning"
    - "Food and workout cards are swipeable (HorizontalPager) with dot indicators hinting at page 2"
    - "Tapping date title on food/workout screens opens DatePicker for date jumping"
  artifacts:
    - path: "app/src/main/java/com/sakura/features/dashboard/DashboardScreen.kt"
      provides: "Home tab screen with two pager cards and sync status"
      min_lines: 80
    - path: "app/src/main/java/com/sakura/features/dashboard/DashboardViewModel.kt"
      provides: "Multi-repository aggregation for today's food + workout + sync status"
      exports: ["DashboardViewModel"]
    - path: "app/src/main/java/com/sakura/features/dashboard/DashboardUiState.kt"
      provides: "Today state and weekly analytics state models"
    - path: "app/src/main/java/com/sakura/features/dashboard/SyncStatusBadge.kt"
      provides: "Green pill composable with sync timestamp and conflict warning"
    - path: "app/src/main/java/com/sakura/navigation/AppNavHost.kt"
      provides: "MainScaffold with shared NavigationBar wrapping all tab routes"
  key_links:
    - from: "DashboardViewModel"
      to: "FoodRepository + WorkoutRepository"
      via: "loadDay() and loadSession() for today's date"
      pattern: "foodRepo\\.loadDay|workoutRepo\\.loadSession"
    - from: "DashboardViewModel"
      to: "SyncthingFileBackend"
      via: "checkSyncStatus() for conflict detection"
      pattern: "syncBackend\\.checkSyncStatus"
    - from: "AppNavHost MainScaffold"
      to: "NavigationBar"
      via: "shared bottomBar across all tab destinations"
      pattern: "NavigationBar.*NavigationBarItem"
---

<objective>
TodayScreen with unified macro + workout summary, navigation polish, and sync status indicator.

Purpose: Delivers the Phase 4 home screen — the single view that summarizes the user's day at a glance. Also restructures navigation to use a shared MainScaffold (removing duplicated NavigationBar from individual screens) and adds DatePicker date-jumping to food/workout screens.

Output: DashboardScreen with two HorizontalPager cards (food progress + workout summary), DashboardViewModel aggregating both repos, SyncStatusBadge, MainScaffold with shared NavigationBar, DatePicker on date titles.
</objective>

<execution_context>
@/Users/marcosandrade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcosandrade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-polish/04-CONTEXT.md
@.planning/phases/04-dashboard-and-polish/04-RESEARCH.md
@app/src/main/java/com/sakura/navigation/AppNavHost.kt
@app/src/main/java/com/sakura/navigation/Routes.kt
@app/src/main/java/com/sakura/di/AppContainer.kt
@app/src/main/java/com/sakura/sync/SyncBackend.kt
@app/src/main/java/com/sakura/sync/SyncthingFileBackend.kt
@app/src/main/java/com/sakura/data/food/FoodRepository.kt
@app/src/main/java/com/sakura/data/food/FoodEntry.kt
@app/src/main/java/com/sakura/data/food/MealGroup.kt
@app/src/main/java/com/sakura/data/workout/WorkoutRepository.kt
@app/src/main/java/com/sakura/data/workout/WorkoutSession.kt
@app/src/main/java/com/sakura/data/workout/ExerciseLog.kt
@app/src/main/java/com/sakura/features/foodlog/FoodLogScreen.kt
@app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
@app/src/main/java/com/sakura/ui/theme/Color.kt
@gradle/libs.versions.toml
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data layer, navigation restructure, and ViewModel</name>
  <files>
    gradle/libs.versions.toml
    app/build.gradle.kts
    app/src/main/java/com/sakura/navigation/Routes.kt
    app/src/main/java/com/sakura/sync/SyncBackend.kt
    app/src/main/java/com/sakura/sync/SyncthingFileBackend.kt
    app/src/main/java/com/sakura/features/dashboard/DashboardUiState.kt
    app/src/main/java/com/sakura/features/dashboard/DashboardViewModel.kt
    app/src/main/java/com/sakura/di/AppContainer.kt
    app/src/main/java/com/sakura/navigation/AppNavHost.kt
    app/src/main/java/com/sakura/features/foodlog/FoodLogScreen.kt
    app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
  </files>
  <action>
    **1. Add Vico dependency:**
    - In `libs.versions.toml`: add `vico = "3.1.0"` to [versions], add `vico-compose-m3 = { group = "com.patrykandpatrick.vico", name = "compose-m3", version.ref = "vico" }` to [libraries]
    - In `app/build.gradle.kts`: add `implementation(libs.vico.compose.m3)`

    **2. Add Home route:**
    - In `Routes.kt`: add `@Serializable object Home` (the dashboard/today screen route)

    **3. Add sync status detection to SyncBackend:**
    - In `SyncBackend.kt`: add `suspend fun checkSyncStatus(): SyncStatus` to the interface. Add a `data class SyncStatus(val lastSyncedAt: Long?, val hasConflicts: Boolean, val folderAccessible: Boolean)` either in the same file or a separate `SyncStatus.kt`.
    - In `SyncthingFileBackend.kt`: implement `checkSyncStatus()` — call `folder.listFiles()` directly (NOT `listOrgFiles()` which filters conflicts out), check for `.sync-conflict` in filenames, get `maxOfOrNull { it.lastModified() }` from non-conflict .org files. Return `SyncStatus(lastModified, hasConflicts, folderAccessible=true)`. Handle missing folder path as `SyncStatus(null, false, false)`.
    - IMPORTANT: Do NOT use `listOrgFiles()` for conflict detection — it intentionally filters `.sync-conflict` files out (Pitfall 6 from RESEARCH.md).

    **4. Create DashboardUiState:**
    - `DashboardUiState.kt` in `features/dashboard/`:
      ```
      data class DashboardTodayState(
          val isLoading: Boolean = true,
          // Food
          val totalCalories: Int = 0,
          val totalProtein: Int = 0,
          val totalCarbs: Int = 0,
          val totalFat: Int = 0,
          val targetCalories: Int = 2200,
          val targetProtein: Int = 150,
          val targetCarbs: Int = 250,
          val targetFat: Int = 70,
          val recentFoodDays: List<RecentDay> = emptyList(),  // last 3 days
          // Workout
          val splitDay: SplitDay? = null,
          val templateName: String? = null,
          val exercises: List<ExerciseLog> = emptyList(),
          val isWorkoutComplete: Boolean = false,
          val hasWorkout: Boolean = false,
          val recentWorkoutDays: List<RecentWorkoutDay> = emptyList(),
          // Sync
          val syncStatus: SyncStatus = SyncStatus(null, false, false)
      )
      data class RecentDay(val date: LocalDate, val totalCalories: Int)
      data class RecentWorkoutDay(val date: LocalDate, val splitName: String?, val isComplete: Boolean)
      ```
    - Also add `WeeklyAnalyticsState` for Plan 02 to use later:
      ```
      data class WeeklyAnalyticsState(
          val isLoading: Boolean = false,
          val macroData: List<DailyMacros> = emptyList(),
          val volumeData: List<DailyVolume> = emptyList(),
          val selectedWeeks: Int = 1
      )
      data class DailyMacros(val date: LocalDate, val protein: Int, val carbs: Int, val fat: Int)
      data class DailyVolume(val date: LocalDate, val volume: Double, val trendValue: Double = 0.0)
      ```

    **5. Create DashboardViewModel:**
    - `DashboardViewModel.kt` in `features/dashboard/`:
    - Constructor takes `foodRepo: FoodRepository`, `workoutRepo: WorkoutRepository`, `prefsRepo: AppPreferencesRepository`, `syncBackend: SyncBackend`
    - Expose `val today: StateFlow<DashboardTodayState>` and `val weekly: StateFlow<WeeklyAnalyticsState>`
    - On init, load today's food data via `foodRepo.loadDay(LocalDate.now())`, today's workout via `workoutRepo.loadSession(LocalDate.now())`, macro targets from `prefsRepo`, and sync status from `syncBackend.checkSyncStatus()`
    - Load recent 3 days for food (load days for yesterday, day before, etc.) and recent 3 workout sessions from `workoutRepo.loadHistory().take(3)`
    - All loading on `Dispatchers.IO` via `viewModelScope.launch`
    - Use `ViewModel.factory()` companion pattern matching FoodLogViewModel and WorkoutLogViewModel established patterns
    - Add `fun loadWeekly(weeks: Int)` stub that sets `_weekly.value = WeeklyAnalyticsState(selectedWeeks = weeks)` — Plan 02 will implement the full aggregation
    - Add `fun refresh()` to reload today's data (for when user returns to home tab)

    **6. Restructure navigation — MainScaffold with shared NavigationBar:**
    - In `AppNavHost.kt`: wrap the post-onboarding navigation in a `MainScaffold` composable that provides a shared `NavigationBar` as `bottomBar` in a `Scaffold`. The `NavHost` goes inside the scaffold content area with `innerPadding`.
    - The MainScaffold NavigationBar has 4 items: FOOD, WORKOUT, HOME, SETTINGS — use the same icons as currently in FoodLogScreen/WorkoutLogScreen (Icons.Filled.DateRange for Food, Icons.Filled.Star for Workout, Icons.Filled.Home for Home, Icons.Filled.Settings for Settings).
    - Use canonical M3 tab nav pattern: `popUpTo(navController.graph.findStartDestination().id) { saveState = true }`, `launchSingleTop = true`, `restoreState = true` on every tab click.
    - Set `Home` as the startDestination (not FoodLog) so the home screen is the first thing users see after onboarding.
    - Track `currentRoute` from `navController.currentBackStackEntryAsState()` to highlight the active tab.
    - NavigationBarItem colors: use `NavigationBarItemDefaults.colors(indicatorColor = CherryBlossomPink, selectedIconColor = Color.White, selectedTextColor = CherryBlossomPink)` for the selected item (matching existing pattern).
    - Add the `Home` composable route inside NavHost that creates DashboardViewModel via `viewModel(factory = DashboardViewModel.factory(...))` and renders `DashboardScreen(...)`.
    - Keep onboarding flow outside the MainScaffold (it navigates to MainScaffold on completion).

    **7. Remove per-screen NavigationBar from FoodLogScreen and WorkoutLogScreen:**
    - In `FoodLogScreen.kt`: remove the entire `bottomBar = { NavigationBar(...) { ... } }` block from the Scaffold. Remove the `onNavigateToWorkout` parameter. Keep `onNavigateToSettings` (it navigates to a non-tab Settings screen, which should still work as a push navigation from the tab).
    - In `WorkoutLogScreen.kt`: remove the entire `WorkoutBottomBar` composable and the `bottomBar = { WorkoutBottomBar(...) }` from Scaffold. Remove the `onNavigateToFoodLog` parameter. Keep `onNavigateToSettings` and `onNavigateToHistory`.
    - Remove all NavigationBar/NavigationBarItem imports from both files.
    - Update AppNavHost: remove `onNavigateToWorkout` from FoodLogScreen call, remove `onNavigateToFoodLog` from WorkoutLogScreen call.

    **8. Wire AppContainer:**
    - The `syncBackend` is already exposed on `AppContainer`. DashboardViewModel needs `foodRepository`, `workoutRepository`, `prefsRepo`, and `syncBackend` — all already available on AppContainer. No changes to AppContainer needed unless you want a convenience accessor (not required).

    **Build verification:**
    - Run `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug` from project root to confirm compilation.
    - Fix any compilation errors before proceeding.
  </action>
  <verify>
    `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug` compiles without errors.
  </verify>
  <done>
    Vico dependency added. Home route exists. SyncBackend has checkSyncStatus(). DashboardViewModel loads today's food+workout+sync data. AppNavHost uses MainScaffold with shared NavigationBar. Per-screen NavigationBar removed from FoodLogScreen and WorkoutLogScreen. App compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: DashboardScreen UI with pager cards, sync badge, and DatePicker</name>
  <files>
    app/src/main/java/com/sakura/features/dashboard/DashboardScreen.kt
    app/src/main/java/com/sakura/features/dashboard/FoodProgressCard.kt
    app/src/main/java/com/sakura/features/dashboard/WorkoutSummaryCard.kt
    app/src/main/java/com/sakura/features/dashboard/SyncStatusBadge.kt
    app/src/main/java/com/sakura/features/foodlog/FoodLogScreen.kt
    app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
  </files>
  <action>
    **1. Create SyncStatusBadge.kt:**
    - Green pill composable: `Surface(shape = RoundedCornerShape(16.dp), color = ForestGreen)` containing a Row with checkmark Icon + "Synced" text.
    - When tapped (clickable modifier), show a simple tooltip/popup or Snackbar with the last-synced timestamp (formatted as "Last synced: 2:15 PM") or "Conflict detected" warning if `hasConflicts` is true.
    - If `folderAccessible` is false, show an orange pill "Offline" instead.
    - If `hasConflicts` is true, show a yellow/warning pill "Conflict" with warning icon.

    **2. Create FoodProgressCard.kt (page 1 of food pager):**
    - Big calorie number: `Text("1,420", fontSize = 36.sp, fontWeight = Bold)` with "/ 2,200 kcal" in smaller muted text and "780 left" in green below.
    - Full-width `LinearProgressIndicator` for calories with `progress = (logged / target).coerceIn(0f, 1f)` and CherryBlossomPink color.
    - Three equal macro mini-cards in a `Row`: Protein (ForestGreen), Carbs (WarmBrown), Fat (DeepRose) — each with value "132g", target "/150g", and a colored `LinearProgressIndicator`.
    - "Recent" row at bottom: Row of last 3 days, each showing abbreviated date ("Apr 10") + calorie total. Use muted text. If no data, show "No recent data".
    - Use `NumberFormat.getNumberInstance()` for comma-formatted calorie display.

    **3. Create WorkoutSummaryCard.kt (page 1 of workout pager):**
    - Header: "Workout" text with a dumbbell icon (use Icons.Filled.FitnessCenter if available, else Icons.Filled.Star) in CherryBlossomPink.
    - Split info row: "Monday - Lift" (from templateName) with a status badge — "In progress" (CherryBlossomPink), "Complete" (ForestGreen), or "Rest" (MediumGray) if no workout exists.
    - Exercise list: up to 4 exercise rows, each showing exercise name, set progress ("2 of 4 sets"), and weight. Use `LazyColumn` only if needed; a simple `Column` with `.take(4)` is preferred (avoid nesting lazy layouts).
    - "Recent" row at bottom: last 3 workout days with date + split name + checkmark if complete. Same style as food recent row.
    - If no workout today (`hasWorkout == false`), show a rest day card with "Rest Day" text and a relaxed illustration or muted text.

    **4. Create DashboardScreen.kt:**
    - Takes `viewModel: DashboardViewModel` parameter.
    - Collect `viewModel.today.collectAsStateWithLifecycle()`.
    - Header: "Good morning" greeting (or "Good afternoon"/"Good evening" based on hour) + "Today, Apr 12" date text. SyncStatusBadge aligned to top-right.
    - Vertical scroll `Column` (NOT LazyColumn — only 2 cards):
      - **Food card**: `ElevatedCard` containing a `HorizontalPager` with 2 pages. Page 0 = `FoodProgressCard(...)`. Page 1 = empty placeholder `Box` with "Weekly Macros" text (Plan 02 will fill this with the Vico chart). Dot indicators at bottom (2 dots, active dot = `MaterialTheme.colorScheme.primary`, inactive = `outlineVariant`).
      - **Workout card**: Same pager pattern. Page 0 = `WorkoutSummaryCard(...)`. Page 1 = empty placeholder for volume chart.
    - Each card has a fixed height (food card: ~320.dp, workout card: ~300.dp) to prevent pager measurement issues (Pitfall 4 from RESEARCH.md).
    - Wrap each `HorizontalPager` in `remember { rememberPagerState(pageCount = { 2 }) }`. IMPORTANT: use `rememberPagerState` (NOT just `PagerState()`) per stable API.
    - Card padding: 16.dp horizontal, 8.dp vertical between cards.
    - On `LaunchedEffect(Unit)` or `onResume`, call `viewModel.refresh()` so data reloads when returning to home tab.

    **5. Add DatePicker to FoodLogScreen and WorkoutLogScreen:**
    - In `FoodLogScreen.kt`: make the date title Text clickable. On click, show `DatePickerDialog` (Material3) pre-selected to current date. On confirm, call `viewModel.navigateToDate(selectedDate)`. Add `@OptIn(ExperimentalMaterial3Api::class)` annotation. Use `rememberDatePickerState(initialSelectedDateMillis = ...)` with epoch millis conversion. Add a `var showDatePicker by remember { mutableStateOf(false) }` flag.
    - In `WorkoutLogScreen.kt`: same pattern — make date title clickable, show DatePickerDialog, navigate to selected date.
    - FoodLogViewModel and WorkoutLogViewModel already have date navigation (they use `_selectedDate` StateFlow). The DatePicker just calls the same navigation function with the picked date instead of +/- 1 day.

    **Build and verify:**
    - Run `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug` to confirm compilation.
  </action>
  <verify>
    `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug` compiles without errors.
  </verify>
  <done>
    DashboardScreen renders with food progress card and workout summary card in HorizontalPager layout with dot indicators. SyncStatusBadge shows sync state. Page 2 of each pager has placeholder for charts (Plan 02). DatePicker available on food/workout date titles. App compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Home screen (Dashboard) with today's food macro progress and workout summary in swipeable cards, shared bottom navigation across all tabs, sync status badge, and date picker on food/workout screens.
  </what-built>
  <how-to-verify>
    1. Build and install: `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew installDebug`
    2. Launch app — should open to HOME tab (not food)
    3. Verify bottom navigation: tap FOOD, WORKOUT, HOME, SETTINGS tabs — each navigates without back-stack issues. Press back from any tab — should exit app (not cycle through tabs)
    4. On HOME tab: verify food progress card shows today's logged macros vs targets. Verify workout summary card shows today's workout status (or rest day if none)
    5. Swipe food card left — should show page 2 placeholder ("Weekly Macros"). Dot indicators should update
    6. Swipe workout card left — should show page 2 placeholder ("Weekly Volume"). Dot indicators should update
    7. Check sync status badge in header — should show green "Synced" pill. Tap it for timestamp
    8. On FOOD tab: tap the date title text — DatePicker should open. Select a past date — food log should navigate to that date
    9. On WORKOUT tab: tap the date title — DatePicker should open and navigate to selected date
    10. Navigate between tabs — state should be preserved (food date selection persists when switching to workout and back)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- App compiles with `assembleDebug`
- Home tab is the default destination after onboarding
- NavigationBar is shared across all tabs (not duplicated per screen)
- Tab switches use popUpTo + saveState + restoreState (no back-stack accumulation)
- DashboardViewModel aggregates from FoodRepository + WorkoutRepository + SyncBackend
- HorizontalPager cards have fixed height (no measurement crashes)
- SyncStatusBadge uses separate checkSyncStatus() (not listOrgFiles())
- DatePicker uses @OptIn(ExperimentalMaterial3Api::class)
</verification>

<success_criteria>
- HOME tab shows today's macro progress (logged kcal, protein, carbs, fat vs targets) and today's workout status
- Bottom nav works across FOOD, WORKOUT, HOME, SETTINGS without back-stack accumulation
- Cards are swipeable with dot indicators (page 2 shows placeholders for now)
- Sync status badge visible in home header
- Date title tap opens DatePicker on food and workout screens
- Existing food and workout logging flows continue to work unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-polish/04-01-SUMMARY.md`
</output>
