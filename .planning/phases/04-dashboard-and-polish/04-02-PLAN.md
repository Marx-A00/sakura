---
phase: 04-dashboard-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/src/main/java/com/sakura/features/dashboard/FoodWeeklyCard.kt
  - app/src/main/java/com/sakura/features/dashboard/WorkoutVolumeCard.kt
  - app/src/main/java/com/sakura/features/dashboard/DashboardViewModel.kt
  - app/src/main/java/com/sakura/features/dashboard/DashboardScreen.kt
  - app/src/main/java/com/sakura/features/dashboard/DashboardUiState.kt
  - app/src/main/java/com/sakura/features/workoutlog/SplitCalendar.kt
  - app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
  - app/src/main/java/com/sakura/features/workoutlog/WorkoutLogViewModel.kt
  - app/src/main/java/com/sakura/features/workoutlog/WorkoutLogUiState.kt
autonomous: false

must_haves:
  truths:
    - "Swiping food card to page 2 shows a grouped bar chart of daily macros (protein/carbs/fat) for the selected time range"
    - "Swiping workout card to page 2 shows a combo bar+line chart with daily volume bars and a trend line"
    - "User can switch between 1W, 2W, and 4W time ranges on both charts"
    - "Training split calendar in the workout tab shows a 4-week rolling grid with split labels and completion status"
    - "Weekly averages row below macro chart shows average protein, carbs, and fat for the time range"
    - "Chart cards show a loading indicator while weekly data loads concurrently on init"
  artifacts:
    - path: "app/src/main/java/com/sakura/features/dashboard/FoodWeeklyCard.kt"
      provides: "Grouped bar chart of daily macros using Vico ColumnCartesianLayer"
      min_lines: 60
    - path: "app/src/main/java/com/sakura/features/dashboard/WorkoutVolumeCard.kt"
      provides: "Combo bar+line chart using Vico ColumnCartesianLayer + LineCartesianLayer"
      min_lines: 60
    - path: "app/src/main/java/com/sakura/features/workoutlog/SplitCalendar.kt"
      provides: "4-week rolling grid calendar with split labels and color coding"
      min_lines: 50
  key_links:
    - from: "DashboardViewModel.loadWeekly()"
      to: "FoodRepository.loadDay() + WorkoutRepository.loadHistory()"
      via: "Multi-day aggregation on Dispatchers.IO"
      pattern: "foodRepo\\.loadDay|workoutRepo\\.loadHistory"
    - from: "FoodWeeklyCard"
      to: "CartesianChartModelProducer"
      via: "LaunchedEffect pushes data via runTransaction"
      pattern: "modelProducer\\.runTransaction"
    - from: "WorkoutVolumeCard"
      to: "CartesianChartModelProducer"
      via: "LaunchedEffect pushes column + line data"
      pattern: "columnSeries|lineSeries"
    - from: "SplitCalendar"
      to: "WorkoutLogViewModel"
      via: "calendarDays StateFlow with 28 CalendarDay entries"
      pattern: "calendarDays|CalendarDay"
    - from: "FoodWeeklyCard/WorkoutVolumeCard"
      to: "WeeklyAnalyticsState.isLoading"
      via: "Show CircularProgressIndicator when isLoading is true"
      pattern: "isLoading.*CircularProgressIndicator|if.*isLoading"
---

<objective>
Weekly macro averages chart, workout volume trends chart, and training split calendar.

Purpose: Fills in the analytics page 2 of each dashboard card with real Vico charts, adds the training split calendar to the workout tab, and completes the data aggregation logic for weekly views. This finishes all Phase 4 visual analytics requirements.

Output: FoodWeeklyCard (Vico grouped bar chart), WorkoutVolumeCard (Vico combo bar+line), SplitCalendar (4-week grid), weekly aggregation in DashboardViewModel.
</objective>

<execution_context>
@/Users/marcosandrade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marcosandrade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-polish/04-CONTEXT.md
@.planning/phases/04-dashboard-and-polish/04-RESEARCH.md
@.planning/phases/04-dashboard-and-polish/04-01-SUMMARY.md
@app/src/main/java/com/sakura/features/dashboard/DashboardViewModel.kt
@app/src/main/java/com/sakura/features/dashboard/DashboardUiState.kt
@app/src/main/java/com/sakura/features/dashboard/DashboardScreen.kt
@app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
@app/src/main/java/com/sakura/features/workoutlog/WorkoutLogViewModel.kt
@app/src/main/java/com/sakura/features/workoutlog/WorkoutLogUiState.kt
@app/src/main/java/com/sakura/data/food/FoodRepository.kt
@app/src/main/java/com/sakura/data/workout/WorkoutRepository.kt
@app/src/main/java/com/sakura/data/workout/WorkoutSession.kt
@app/src/main/java/com/sakura/data/workout/SplitDay.kt
@app/src/main/java/com/sakura/ui/theme/Color.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weekly aggregation logic and Vico chart cards</name>
  <files>
    app/src/main/java/com/sakura/features/dashboard/DashboardViewModel.kt
    app/src/main/java/com/sakura/features/dashboard/DashboardUiState.kt
    app/src/main/java/com/sakura/features/dashboard/FoodWeeklyCard.kt
    app/src/main/java/com/sakura/features/dashboard/WorkoutVolumeCard.kt
    app/src/main/java/com/sakura/features/dashboard/DashboardScreen.kt
  </files>
  <action>
    **1. Implement DashboardViewModel.loadWeekly():**
    - Takes `weeks: Int` (1, 2, or 4). Compute date range: `today - (weeks * 7) + 1` to `today`.
    - Set `_weekly.value = _weekly.value.copy(isLoading = true, selectedWeeks = weeks)` FIRST, before launching the IO coroutine. This ensures chart composables show loading state immediately on tab switch.
    - Load food data: call `foodRepo.loadDay(date)` for each date in range inside a single `withContext(Dispatchers.IO)` block. Sum each day's MealGroup entries into `DailyMacros(date, protein, carbs, fat)`. Start with individual loadDay calls per RESEARCH.md recommendation.
    - Load workout data: call `workoutRepo.loadHistory()` once, filter to sessions within date range. Map each to `DailyVolume(date, totalVolume, trendValue)`. Compute trend as a simple moving average (3-day window): for each day, average the volume of that day and the 2 preceding days that have data. For days with no workout, volume = 0 and trendValue carries forward from the last computed value.
    - Compute averages: `avgProtein = macroData.map { it.protein }.average().toInt()`, same for carbs and fat. Add these to WeeklyAnalyticsState.
    - Update `WeeklyAnalyticsState` data class: add `avgProtein: Int = 0`, `avgCarbs: Int = 0`, `avgFat: Int = 0`.
    - Update `_weekly` StateFlow with the computed data AND set `isLoading = false` when complete.
    - Call `loadWeekly(1)` on init (default to 1 week). This runs concurrently with `loadToday()` on init -- both launch separate coroutines on `Dispatchers.IO`.
    - Guard against empty data: if no food/workout data exists for the range, set empty lists and `isLoading = false` (charts will show empty state).

    **2. Create FoodWeeklyCard.kt:**
    - Title: "Weekly Macros" in bold.
    - **Loading state**: If `weeklyState.isLoading` is true, show a centered `CircularProgressIndicator(color = CherryBlossomPink)` instead of the chart content. This handles the concurrent init load where `loadWeekly(1)` runs alongside `loadToday()`.
    - **Segmented time tabs**: Row of 3 `FilterChip` or `SegmentedButton` composables: "1W", "2W", "4W". Selected state driven by `weeklyState.selectedWeeks`. On click, call `onWeeksChanged(weeks)` callback. Tabs remain visible even during loading.
    - **Color legend**: Row with 3 items: green circle + "Protein", coral circle + "Carbs", brown circle + "Fat".
    - **Grouped bar chart**: Use Vico `CartesianChartHost` with `rememberCartesianChart()`.
      - One `rememberColumnCartesianLayer()` with `ColumnCartesianLayer.ColumnProvider.series(...)` providing 3 `rememberLineComponent(color = ...)` instances: ForestGreen, WarmBrown (use a coral color for carbs -- check CONTEXT.md: "coral for carbs"), DeepRose for fat (CONTEXT says "warm brown for fat" -- match exactly: green for protein, coral for carbs, warm brown for fat).
      - IMPORTANT: Use `MergeMode.Grouped` (not stacked) via `mergeMode = { ColumnCartesianLayer.MergeMode.Grouped }`.
      - Bottom axis: day labels (M, T, W, T, F, S, S).
      - Start axis: gram values.
      - IMPORTANT: Wrap `CartesianChartModelProducer` in `remember { CartesianChartModelProducer() }` -- do NOT recreate on recomposition (Pitfall 2).
      - Push data in `LaunchedEffect(macroData)`: `modelProducer.runTransaction { columnSeries { series(...protein values...) }; columnSeries { series(...carbs values...) }; columnSeries { series(...fat values...) } }`
    - **Averages row**: 3 mini Surface cards: "Avg Protein: 132g", "Avg Carbs: 218g", "Avg Fat: 61g" using the computed averages from WeeklyAnalyticsState.
    - Handle empty state: if `macroData` is empty AND `isLoading` is false, show centered "No data for this period" text instead of chart.

    **3. Create WorkoutVolumeCard.kt:**
    - Title: "Weekly Volume" in bold.
    - **Loading state**: Same pattern as FoodWeeklyCard -- show `CircularProgressIndicator(color = CherryBlossomPink)` when `weeklyState.isLoading` is true.
    - Same segmented time tabs as FoodWeeklyCard. Tabs remain visible during loading.
    - **Legend**: pink square + "Volume (kg)", green line icon + "Trend".
    - **Combo chart**: `CartesianChartHost` with `rememberCartesianChart()` containing both `rememberColumnCartesianLayer()` (pink bars for daily volume) AND `rememberLineCartesianLayer()` (ForestGreen trend line) in the same chart call.
      - Column colors: CherryBlossomPink for bars. Use `ColumnProvider.series(rememberLineComponent(color = CherryBlossomPink, thickness = 16.dp))`.
      - Line color: ForestGreen.
      - Push data in `LaunchedEffect(volumeData)`: `modelProducer.runTransaction { columnSeries { series(...volume values...) }; lineSeries { series(...trend values...) } }`
      - Bottom axis: day labels. Start axis: kg values.
    - Handle empty state: if `volumeData` is empty AND `isLoading` is false, show "No workouts this period" text.

    **4. Wire charts into DashboardScreen:**
    - Replace the page 2 placeholder in the food card pager with `FoodWeeklyCard(weeklyState = weeklyState, onWeeksChanged = { viewModel.loadWeekly(it) })`.
    - Replace the page 2 placeholder in the workout card pager with `WorkoutVolumeCard(weeklyState = weeklyState, onWeeksChanged = { viewModel.loadWeekly(it) })`.
    - Collect `viewModel.weekly.collectAsStateWithLifecycle()` in DashboardScreen and pass to both cards.

    **Build verification:**
    - Run `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug`.
    - If Vico API has changed from RESEARCH.md examples, consult the actual 3.1.0 API from the dependency (check import completions). The key classes are in `com.patrykandpatrick.vico.compose.cartesian` and `com.patrykandpatrick.vico.core.cartesian`.
  </action>
  <verify>
    `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug` compiles without errors.
  </verify>
  <done>
    Weekly aggregation loads food and workout data for 1/2/4 week ranges with proper isLoading state transitions. FoodWeeklyCard shows loading spinner then grouped bar chart with protein/carbs/fat per day and averages row. WorkoutVolumeCard shows loading spinner then combo bar+line chart with volume and trend. Both cards wired into DashboardScreen pager page 2. Time range tabs switch between 1W/2W/4W. App compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Training split calendar in workout tab</name>
  <files>
    app/src/main/java/com/sakura/features/workoutlog/SplitCalendar.kt
    app/src/main/java/com/sakura/features/workoutlog/WorkoutLogViewModel.kt
    app/src/main/java/com/sakura/features/workoutlog/WorkoutLogUiState.kt
    app/src/main/java/com/sakura/features/workoutlog/WorkoutLogScreen.kt
  </files>
  <action>
    **1. Add CalendarDay model:**
    - In `WorkoutLogUiState.kt`, add:
      ```
      data class CalendarDay(
          val date: LocalDate,
          val splitDay: SplitDay? = null,     // null = rest day or no data
          val splitLabel: String? = null,      // e.g., "Push", "Pull", "Legs"
          val isComplete: Boolean = false,
          val isPast: Boolean = true,          // false for future days
          val isToday: Boolean = false
      )
      ```

    **2. Add calendar data loading to WorkoutLogViewModel:**
    - Add `private val _calendarDays = MutableStateFlow<List<CalendarDay>>(emptyList())`
    - Expose `val calendarDays: StateFlow<List<CalendarDay>> = _calendarDays.asStateFlow()`
    - Add `fun loadCalendar()` that computes 28 days: 14 days in the past + today + 13 days in the future (4 weeks total, centered roughly on current week).
      - Align to week boundaries: find the Monday 2 weeks ago, take 28 days from there.
      - Load workout history via `workoutRepo.loadHistory()`. Build a map of `LocalDate -> WorkoutSession`.
      - For past days: look up the session for that date. If exists, populate splitDay, splitLabel (from templateName or splitDay.name), isComplete. If no session, it's a rest day (splitDay = null).
      - For future days: use the workout template rotation to predict which split day falls on each future date. Load templates from the existing `WorkoutTemplates` object -- get the 4-day split rotation. Compute which day in the cycle each future date corresponds to (based on the last completed workout's split day position). If prediction is uncertain, leave as null.
      - Set `isToday = (date == LocalDate.now())`, `isPast = (date < LocalDate.now())`.
    - Call `loadCalendar()` on ViewModel init.

    **3. Create SplitCalendar.kt:**
    - Takes `days: List<CalendarDay>` (28 items) and `modifier: Modifier`.
    - **Header row**: 7 day-of-week labels (M, T, W, T, F, S, S) in MediumGray, equal weight.
    - **4 week rows**: each row is a `Row` with 7 `CalendarCell` composables.
    - **CalendarCell**: a `Column` inside a `Box` with `weight(1f)`:
      - Date number (e.g., "12") in small text.
      - Split label (e.g., "Push") in tiny text, colored by split type. Use a color mapping: push = CherryBlossomPink, pull = ForestGreen, legs = WarmBrown, compound/other = DeepRose. If no split, show nothing.
      - Completion checkmark: small green checkmark icon if `isComplete == true`.
      - Today highlight: if `isToday`, add a circle background behind the date number using `MaterialTheme.colorScheme.primary`.
      - Future days: use `alpha = 0.4f` to fade them.
      - Past rest days: show just the date number, no split label.
    - Do NOT use LazyVerticalGrid (Pitfall from RESEARCH.md -- cannot embed in scrollable parents). Use regular `Column { Row { ... } }`.
    - Card wrapper: wrap the entire calendar in an `ElevatedCard` with title "Training Calendar" and 16.dp padding.

    **4. Wire SplitCalendar into WorkoutLogScreen:**
    - Add `SplitCalendar` above or below the existing workout day content in WorkoutLogScreen.
    - Collect `viewModel.calendarDays.collectAsStateWithLifecycle()` and pass to `SplitCalendar(days = calendarDays)`.
    - Position: place the calendar card at the top of the scrollable content, above the exercise list. The calendar should be collapsible or always visible -- choose always visible for simplicity (the card is compact at ~200dp height).

    **Build verification:**
    - Run `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug`.
  </action>
  <verify>
    `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew assembleDebug` compiles without errors.
  </verify>
  <done>
    SplitCalendar composable renders a 4-week grid with day-of-week headers, date numbers, split labels with color coding, completion checkmarks, and today highlight. WorkoutLogViewModel loads 28 days of calendar data from workout history. Calendar is wired into WorkoutLogScreen above the exercise list. App compiles.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Weekly analytics charts (macro grouped bar chart, volume combo chart with trend line) on dashboard card page 2, 1W/2W/4W time range switching, and training split calendar in the workout tab.
  </what-built>
  <how-to-verify>
    1. Build and install: `JAVA_HOME=/Users/marcosandrade/.local/jdk/jdk-17.0.18+8/Contents/Home ./gradlew installDebug`
    2. On HOME tab: swipe food card to page 2 -- verify grouped bar chart shows daily macros (3 colored bars per day: green protein, coral carbs, brown fat). Verify averages row shows computed averages below chart.
    3. Tap 2W tab -- chart should reload with 2 weeks of data (loading spinner should briefly appear during reload). Tap 4W -- should show 4 weeks. Verify no UI jank on tab switch.
    4. Swipe workout card to page 2 -- verify combo chart shows pink volume bars with a green trend line overlaid. If few workouts logged, bars may be sparse -- that's OK.
    5. On WORKOUT tab: verify training split calendar appears above the exercise list. Check that:
       - Past days show split labels (Push, Pull, etc.) with color coding and completion checkmarks
       - Today is highlighted
       - Future days are faded with predicted split labels
       - Calendar shows 4 complete weeks aligned to Monday
    6. Log a new food entry on FOOD tab, then switch to HOME -- verify the food card page 1 updates with new totals and page 2 chart includes today's updated data
    7. Verify existing food and workout logging still work correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Vico charts render without flickering (CartesianChartModelProducer in remember{})
- Weekly data loaded on Dispatchers.IO (no main thread file I/O)
- Loading state shown in chart cards during concurrent init and tab switching (isLoading = true before IO launch, false after completion)
- Time range tabs trigger reload without ANR
- Grouped bar chart uses MergeMode.Grouped (not stacked)
- Chart colors match CONTEXT.md: green/coral/brown for macros, pink/green for volume
- SplitCalendar uses Column/Row (not LazyVerticalGrid)
- Calendar aligns to week boundaries (Monday start)
- Empty state handled gracefully on both charts (distinct from loading state)
</verification>

<success_criteria>
- Food card page 2 shows grouped bar chart with protein/carbs/fat bars per day and averages row
- Workout card page 2 shows combo bar+line chart with volume and trend
- Charts show loading indicator during data fetch (not blank/empty state)
- 1W/2W/4W tabs switch time ranges on both charts
- Training split calendar shows 4-week grid with split labels, colors, checkmarks, and today highlight
- All existing food/workout logging functionality unchanged
- No UI jank on chart loading or time range switching
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-polish/04-02-SUMMARY.md`
</output>
