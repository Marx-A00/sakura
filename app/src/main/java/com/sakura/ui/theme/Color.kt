package com.sakura.ui.theme

import androidx.compose.ui.graphics.Color

// Sakura pink/rose palette — dark theme identity
val Rose400 = Color(0xFFFB7185)       // Primary — main brand pink
val Rose900 = Color(0xFF881337)       // PrimaryContainer — deep rose for containers
val Rose200 = Color(0xFFFECDD3)       // Secondary — soft pink for secondary elements
val RoseOnPrimary = Color(0xFF1A0010) // OnPrimary — text/icons on rose400 background

// Dark surface palette
val DarkBackground = Color(0xFF1A1A1A) // Background — near-black dark gray
val DarkSurface = Color(0xFF242424)    // Surface — slightly lighter dark gray
val DarkSurfaceVariant = Color(0xFF2E2E2E) // SurfaceVariant

// Text/content colors
val NearWhite = Color(0xFFF5F5F5)     // OnBackground and OnSurface — primary text
val MediumGray = Color(0xFFAAAAAA)    // OnSurfaceVariant — secondary text
